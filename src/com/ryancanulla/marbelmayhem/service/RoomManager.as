package com.litl.marbelmayhem.service
{
	import com.adobe.rtc.session.ConnectSession;

	public class RoomManager
	{
		private var _session:ConnectSession;
		
		public function RoomManager(e:ConnectSession) {
			//_session = e;
			///			addListeners();
		}
		
		private function addListeners():void {
			//_session.userManager.userCollection.addEventListener(
		}
	}
}